# Empty = standalone browser demo (no API required).
# C# backend (dotnet run --project backend/Miras.Api):
# VITE_API_URL=http://localhost:5080
VITE_API_URL=
# Supabase Auth. Publishable key is safe for a browser; never use a secret key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
